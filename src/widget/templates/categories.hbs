<div>
    <h1>Categories</h1>
    {{#each categories}}
        <div>
            <a href="javascript:void(0);" id="{{name}}">{{name}}</a>
            <label class="switch">
                <input type="checkbox" onclick="filterControl.filterCategory('{{name}}')" {{#if this.isActive}}checked{{/if}}>
                <span class="slider round"></span>
            </label>
        </div>
    {{/each}}
</div>
